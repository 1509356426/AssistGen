# Study-Log
Record of Large Model Learning

3.5

早上：创建GitHub学习仓库

下午：学习大模型应用开发基础—智能体（Agent）、大语言模型基础、RAG&向量数据库、Agent

晚上：娱乐

总结：效率有点低，还是有点点不坚定，学的比较虚

明天任务：基础看完+部署第一个大模型项目

3.6

大模型基础知识学完

开启项目学习：GraphRAG基础概念+成功本地部署微软开源框架MicroSoft GraphRAG

明日任务：开始背八股+刷Hot100+看GraphRAG源码  理解Workflow工作流

3.7~3.8

掌握MicroSoft GraphRAG 索引构建和 Query构建过程

整理Agent和RAG八股

算法4题

问题：1. 动手实操较少，基本都在看理论，不知道怎么运用 2. 学习方法有问题，时间不多就针对面试学，先保证能应付面试 3. 笔记在精不在多，看完一套框架流程自己总结能口头表述的流程，不要去Copy全部内容



3.9-3.10：心态烂了

3.11：部署好了大模型核心项目+理解了关键流程+算法4题+八股



3.12：

早上：情绪烂+被叫醒收拾情绪

下午：搭建智能体基本架构

Agent两个框架大致流程（Agent流程）

一个**简单**智能体SimpleAgent**（含工具）的**完整生命周期**：首先用户发送消息给到Agent，Agent负责构建上下文消息，先找Memory记忆层获取历史消息来拼接成完整上下文消息，再发送给LLM（大脑）来处理，处理完后来**解析**判断要不要调用工具（条件判断），调用后返回消息给LLM整理上下文后再发送给Agent（LLM二次调用只负责整理最后的回答，转换成自然语言），如此循环，最后由Agent判断不用再调用工具/给LLM后返回结果给用户，并且将上下文信息记录给Memory（History）

- 结果是一次直接输出，不用循环反复的思考和更新提示词让LLM重新思考（LLM只会调用一次）



**ReActAgent** 的 Think-Act-Observe 循环：Agent 首先分析当前的任务和已有的信息发给LLM，基于上一步的思考，LLM 决定调用哪一个具体的**工具（Action）**，Agent 运行工具，并获取工具返回的真实数据（Observation），Agent 将这一轮的“思考+行动+观察”全部存入历史记录。在下一轮循环开始时，换个词再搜一次（再发送给LLM进行进一步优化处理）

- 结果是多次循环调用LLM后反复优化Prompt和思考结果后才返回的



Agent只是一个执行者，是一个抽象概念，它包含了大模型（LLM）+ 规划（Planning）+ 记忆（Memory）+ 工具使用（Tool Use）

五个基类形成一个**完整的依赖链**：Config（配置） 和 Message（上下文信息） 是最底层的**数据**类，LLM 依赖它们，Agent 组合 LLM + Config + Memory(Message[]) + ToolRegistry(Tool[]) 构成完整的**运行单元**。



晚上：重新部署了项目，梳理智能客服项目整体框架逻辑，知道大概的过程（索引如何构的流程、textunit切分、llm提取实体关系是如何做到的）

明天：进一步完善项目细节，找一下看能不能有优化的地方，把项目写入测开简历，明天先投递10份测开，后面在做一份简单的大模型项目同步投递大模型

反思：早上下午情绪反扑摆烂，没刷算法和八股



3.13：

6：00醒，赖床一小时

7：00-8：00：跑步+洗澡

8：00-8：30：吃早饭

9：00到学校

9：00-11：30：效率比较底，看了看算法和八股

11：30-12：00：吃饭

12：00-14：00：看电视+休息

14：00-15：00：源码分析，整个项目的框架比较清晰，就是一个基于LangGraph框架的多Agent系统，其中就定义了节点和边，图的编译，图状态的定义，事件流，工具集成，上下文记忆，路由，父子图等核心的组件。项目中比较核心的就是**提示词工程**和**LangChain组件**的结合运用，其中最底层的多Agent框架及整体流程：

Router路由组件接收用户的消息，通过意图识别模块来识别用户意图并将问题路由给五个核心业务子图模块（一般问题、更多信息问题、查询知识库问题、处理图片、处理文件），处理完后通过结构化输出（定义输出的格式）来进行统一管理，最后给到Hallucinations 节点来进行幻觉检测，如果存在问题则修正，没有问题则返回给用户

- LangGraph和LangChain的具体内容后续可以了解一下



15：00-17：00：和同门聊天去了，效率有点地，看完项目的意图识别模块，整体流程就是通过提示词功能+LLM分解用户信息+借助LangChain 的 with_structured_output 方法将大模型的输出转换为具体的结构化数据，再通过LangGraph定义图的输出格式，最后给到 route_query 节点来找到对应的图（五个子图代表五个核心业务）

- 大致了解五个业务的流程。能说就行



3.14—3.15：摆烂🐕



3.16：

早上：赖床不起，状态不太对，学了AI Conding的前置知识，为后续AI搓项目做准备

下午：看完了智能体客服项目，对整体内容和细节基本了解清楚，相应的高频问题回答细节整理完，后续要加强口语化表达即可

晚上：

学会了大模型文本切分机制，主要包括3大部分：

1. **Tokens**切块：自己可以设置一个块所包含的Tokens数量，自定义块的大小，并且可以通过设置overlap来保证 语句/实体 的完整性，防止出现信息孤岛的情况。块分的小可以更精准的识别信息，块分的大包含更广的上下文信息

2. **Sentence** 策略： 基于**句子边界**切分（句分割：使用 NLTK或者spaCy库提供的句子分割功能），细节是可以做递归分割来限制块的大小
3. **语义**分割：根据向量化后的相似度进行语义层的切割
4. **Markdown/CSV** 策略： 保留文档**结构**的智能切分



实体提取：

1. 离线提取：存数据，通过Cypher 语句（类似SQL，但更容易处理复杂数据，可以存储数据之间的关系）和**Microsoft GraphRAG** 构建离线索引
2. 在线提取：提取用户的问题，用**Text2Cypher 转化**（核心：**Few-shot 示例**+LLM构成字典）



五大查询模块：其中只有**graphrag-query**和**additional-query**需要提前**安全护栏**，其它都没有。且信息交互是存在一个全局State，根据在这里面添加内容来进行数据传递和交互。

GraphRag最核心的优势是能体现实体关系，进行多跳推理，完成更复杂的任务



整体流程、索引构建流程、LLM提取实体、neo4j、意图识别

明天：早上秒起不要赖床，赖床刷手机影响心态和状态。继续学习AI Codding，完善简历开始投递，从小厂开始



3.17（效率低，没学啥内容）

早上：给老登干杂活

下午：改简历 + 投简历

晚上：打游戏



3.18

6：00醒赖床一小时

7：00-8：00：起床+吃早餐

8：00-9：00：学了四个经典的Agent架构范式：Simple Agent、ReAct、Plan-and-Solve、Reflection，看了大致基础代码，了解了大致流程，和各个智能体的优缺点

记忆系统、RAG和向量数据库、工具类调用



3.19—4.27：心态爆了，自卑感拉满，感觉自己哪哪都是问题，哪哪都搞不好，主观能动性太差了，效率极低，心气都被心理问题磨完了
